// Stub for @julusian/freetype2 — the atem-connection package uses this only for
// multiview label rendering, which this app never calls. Replacing the native
// addon with an empty object prevents the pkg snapshot from crashing on startup.
module.exports = {};
